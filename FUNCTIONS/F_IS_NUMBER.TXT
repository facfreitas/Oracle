CREATE OR REPLACE FUNCTION F_IS_NUMBER ( P_NUMERO IN VARCHAR2 ) RETURN  BOOLEAN IS
  NUMERO_AUX NUMBER;
BEGIN
  NUMERO_AUX := TO_NUMBER(P_NUMERO);
  RETURN (TRUE);  
EXCEPTION 
  WHEN OTHERS THEN
    RETURN(FALSE);
END;
/

